<%= form_for(planned_role) do |f| %>
  <% if planned_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planned_role.errors.count, "error") %> prohibited this planned_role from being saved:</h2>

      <ul>
      <% planned_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :associate_id %>
    <%= f.number_field :associate_id %>
  </div>

  <div class="field">
    <%= f.label :role_id %>
    <%= f.number_field :role_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
